import React from 'react';
import { SVG_ICON } from 'svg/enum';
import SvgIcon from 'svg/svgIcon';

const BackingArrow = () => {
    return <SvgIcon icon={SVG_ICON.LEFT_ARROW} />;
};

export default BackingArrow;
