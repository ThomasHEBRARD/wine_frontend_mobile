import React from 'react';
import { SafeAreaView, Text } from 'react-native';

const MyBottles = () => {
    return (
        <SafeAreaView>
            <Text>Search Field (elastic search)</Text>
            <Text>List of bottles (infinite load)</Text>
        </SafeAreaView>
    );
};

export default MyBottles;
